# Tasks: Customer PII Masking

- [x] Update `src/utils/privacy.js` so `maskPhone` shows only the first 2 digits/characters and masks everything else
- [x] Update `AdminTicketCard.jsx` to add `showPii` state and an eye toggle button in the modal's client section
- [x] Conditionally display masked or raw phone and email inside the modal based on `showPii` state
- [x] Verify frontend build with `npm run build`
